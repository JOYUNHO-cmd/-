# 한얼 웹사이트

승인된 cleanworks.kr 공개 사이트를 기반으로 한 정적 재구축입니다.
기존 페이지 경로, 본문, 사진과 스타일을 유지하며 업체명을 한얼로 변경합니다.

## 설계 및 변경 범위

- 공통 HTML 레이아웃, 헤더·푸터와 페이지별 콘텐츠 데이터를 분리합니다.
- 페이지별 메타데이터, 구조화 데이터, 리치 콘텐츠를 보존합니다.
- 사업자 정보는 `추후 입력`, 전화번호는 `000-0000-0000`으로 설정합니다.
- 텍스트형 한얼 로고를 사용하고 기존 인증·분석 코드는 제거합니다.
- 전화번호가 등록되기 전에는 전화 버튼이 실제 통화를 시도하지 않습니다.
- 모바일 메뉴의 키보드 접근성을 보완합니다.
- 비공개 검토용으로 noindex 및 robots 차단을 사용합니다. 실제 공개 전 변경해야 합니다.
- `brand.json`에서 업체 정보를 수정한 후 `python scripts/build.py`로 다시 생성합니다.

## 검증

원본 사이트맵 경로 수, 모든 내부 링크 및 로컬 이미지·스타일 참조,
기존 업체 정보 잔존 여부를 검사합니다. 주요 템플릿은 모바일·데스크톱으로 확인합니다.
공개 사이트만으로 원본 Astro 저장소와 빌드 원본을 복구하는 것은 불가능하므로,
원본과 동등한 정적 HTML을 생성하는 별도 소스로 구성합니다.
